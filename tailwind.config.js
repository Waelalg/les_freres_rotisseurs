/** @type {import('tailwindcss').Config} */
export const content = [
  "./src/**/*.{js,jsx,ts,tsx}", // Ensure it matches your React project's structure
];
export const theme = {
  extend: {},
};
export const plugins = [];
